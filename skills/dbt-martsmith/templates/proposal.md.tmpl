---
status: proposed
source_sheet: <slug>.csv
generated_at: <ISO8601>
naming_convention_detected_from: <dbt-bouncer.yml | sampled:models/marts | none>
---

# <Title> — Mart Proposal

## Summary

| Metric | Column used | Description | Proposed calculation | Proposed tests | Importance | Status |
|---|---|---|---|---|---|---|
| <metric name> | <matched model, or blank> | <description column> | <your inferred calculation> | <e.g. not_null, or "custom: not_negative", or "none (low importance)"> | <low \| medium \| high> | <Matched \| Ambiguous \| Blocked> |
<!-- one row per metric, same order as the sheet -->

## <Metric name>
- Description: <description column>
- Reasoning (stakeholder, verbatim): <reasoning column>
- Proposed calculation (your inference — confirm before building): <derived from reasoning + grounded columns>
- Proposed tests: <list, noting which reuse an existing project test vs. a built-in — see AGENTS.md Step 5>
- Importance: <low | medium | high>
- Grounding: <matched "<model>" | ambiguous - candidates: [...] | blocked - no matching staging/intermediate model found>
- Status: <Matched | Ambiguous | Blocked>

<!-- repeat one section per metric row -->

## Proposed changes (once approved)
- New: `draft__<name>.sql` (grain: <inferred grain statement>) —
  tests calibrated to <importance>
- schema.yml: description from Description, tests/freshness per the
  importance-based rules in AGENTS.md Step 5

## Open questions
- [ ] <one line per Ambiguous/Blocked row, or any other unresolved item>

## Next steps
1. Resolve open questions above.
2. **Reply to approve** — nothing has been built yet, and the calculation
   above is an inference, not what the stakeholder literally wrote. Once
   you confirm, the draft SQL/schema.yml files get written for review.
3. After that: rename (drop the `draft__` prefix), move into the real
   `models/marts/...` directory, and run `dbt parse`/`dbt-bouncer` locally.
