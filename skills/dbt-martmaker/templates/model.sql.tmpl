-- DBT-MARTMAKER DRAFT
-- Generated from <slug>.csv on <ISO8601>. Not part of the dbt DAG until a
-- human reviews this, drops the draft__ prefix, and moves it into
-- models/marts/. Do not run `dbt build` against this file in place.
--
-- Metric: <metric name>
-- Description: <description column, verbatim from the sheet>
-- Reasoning (stakeholder, verbatim): <reasoning column>
-- Proposed calculation (inferred, approved before this file was built): <derived calculation>

with

source_model as (
    select * from {{ ref('<matched_model_name>') }}
),

final as (
    select
        -- TODO: columns implied by the proposed calculation above go here
        *
    from source_model
)

select * from final
